{% extends 'base.html.twig' %}

{% block title %}Task{% endblock %}

{% block body %}
<h1>Task</h1>
<div class"overflow-x-auto border-x border-t">
    <table class="table table-auto w-full my-5">
        <tbody>
            <tr class="border-b hover:bg-gray-50">
                <th class="text-left p-4 font-medium">Id</th>
                <td class="p-4">{{ task.id }}</td>
            </tr>
            <tr class="border-b hover:bg-gray-50">
                <th class="text-left p-4 font-medium">Name</th>
                <td class="p-4">{{ task.name }}</td>
            </tr>
        </tbody>
    </table>
</div>
<a href="{{ path('app_task_index') }}" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 mr-2 mb-2 dark:bg-blue-600 dark:hover:bg-blue-700 focus:outline-none dark:focus:ring-blue-800">back to list</a>

<a href="{{ path('app_task_edit', {'id': task.id}) }}" class="text-white bg-blue-700 hover:bg-blue-800 focus:ring-4 focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 mr-2 mb-2 dark:bg-blue-600 dark:hover:bg-blue-700 focus:outline-none dark:focus:ring-blue-800">edit</a>
<div class="mb-4"></div>
    {{ include('task/_delete_form.html.twig') }}
{% endblock %}
