{% extends 'base.html.twig' %}

{% block title %}TaskSession{% endblock %}

{% block body %}
    <h1>TaskSession</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ task_session.id }}</td>
            </tr>
            <tr>
                <th>IsTaskCompleted</th>
                <td>{{ task_session.isTaskCompleted ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_task_session_index') }}">back to list</a>

    <a href="{{ path('app_task_session_edit', {'id': task_session.id}) }}">edit</a>

    {{ include('task_session/_delete_form.html.twig') }}
{% endblock %}
